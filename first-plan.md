# PolicyGraph Agent 最终产品计划

## 1. 产品定位

最终产品面向 non-tech 用户，不做复杂的 RAG 控制台，也不把用户暴露在 HyDE、Rerank、RRF、token、工具调用等工程细节里。

产品定位：

> PolicyGraph Agent：一个会查条款、做对比、审风险的保险分析助理。

用户感知应该是：

- 我选一个保险产品，它帮我解释清楚。
- 我选两个产品，它帮我做对比。
- 我想知道风险，它帮我审计条款。
- 每个结论都能点开看到原文证据。

技术亮点仍然保留在后台和面试讲述里，但前端只呈现用户真正关心的结果。

## 2. 核心原则

### 2.1 用户体验优先

- 首页直接进入任务，不做复杂 landing page。
- 不让用户理解 RAG、Agent、Embedding、Trace 等概念。
- 但要让用户看见“查了什么、命中了哪些原文、用了多少条证据”，不用技术名词也能感知 RAG 价值。
- 不让用户写复杂 prompt。
- 尽量用选择、按钮、卡片、表格完成主要任务。
- 每一步都要有清晰反馈：正在查找条款、正在生成对比、正在检查风险。

### 2.2 功能少但完整

第一版只保留四个主任务：

1. 深入提问
2. 解读产品
3. 对比产品
4. 审计风险

其他功能如 Corpus、Evaluation、Trace Lab、复杂配置都放到后台或后续版本，不在主界面干扰用户。

### 2.3 Agent 能力要可感知

用户不需要看到工具 schema，但要感受到 Agent 不是普通聊天：

- 它会先识别产品。
- 它会查找相关条款。
- 它会整理证据。
- 它会检查答案是否有依据。
- 它会指出缺失信息和潜在风险。

前端用轻量的“进度步骤”和“证据卡片”体现这些能力。

### 2.4 RAG 价值必须可见

上一版容易给人“把整个险种上下文塞进模型”的感觉，所以最终实现要明确体现：

- 先把任务拆成多个检索问题，例如“等待期”“免责条款”“续保条件”。
- 每个检索问题返回命中的条款编号。
- 结果页显示候选条款数、入模条款数、覆盖率。
- 生成环节只使用检索命中的条款，不默认把完整产品全文塞给模型。
- 对普通用户不展示 Embedding、RRF、HyDE 等术语，只展示“检索过程”。

## 3. 最终前端形态

### 3.1 页面结构

建议主页面只有三块：

```text
顶部：产品名 + 知识库状态

中间：四个任务入口
  - 深入提问
  - 解读产品
  - 对比产品
  - 审计风险

下方：结果区
  - 答案 / 对比表 / 风险报告
  - 引用证据
  - 简洁执行步骤
```

不要默认展示复杂侧边栏。可以保留一个“详情”按钮，给面试或 demo 时展开 Agent 执行过程。

### 3.2 四个任务入口

#### 深入提问

用户操作：

1. 选择一个产品
2. 输入一个具体问题
3. 点击“提交问题”
4. 可直接继续追问，不需要重新选择产品

输出：

- 直接回答
- 回答要点
- 需要注意什么
- 建议继续追问
- 原文引用
- 检索过程

这个入口补足用户的自由探索需求，例如：

- 既往症赔不赔？
- 外购药有什么限制？
- 续保会不会被拒？

回答必须基于当前产品的检索证据，不能把整份条款直接塞给模型。

连续追问规则：

- 同一产品下保留近期问答上下文。
- 用户问“那高血压呢？”时，要结合上一轮“既往症赔不赔？”理解问题。
- 检索仍然必须重新查当前问题相关条款，不能只沿用上一轮答案。
- 回答顶部先给一句话结论，再给原文依据和下一步建议。

#### 解读产品

用户操作：

1. 选择一个产品
2. 点击“生成解读”

输出：

- 这个产品适合谁
- 主要保障
- 不保什么
- 需要注意什么
- 原文引用

适合 non-tech 用户，因为他不需要提问，也能得到有用结果。

#### 对比产品

用户操作：

1. 选择产品 A
2. 选择产品 B
3. 可选填写“我的情况”
4. 点击“开始对比”

输出：

- 对比表格
- 主要差异
- 哪类人更适合哪个产品
- 按用户情况给选择建议
- 证据是否完整

注意：第一版不要开放太多自定义维度。默认维度即可：

- 产品定位
- 核心保障
- 等待期
- 免责条款
- 适合人群
- 主要风险

#### 审计风险

用户操作：

1. 选择一个产品
2. 点击“检查风险”

输出：

- 风险等级：低 / 中 / 高
- 发现的风险点
- 条款缺失项
- 建议追问
- 原文证据

这会最明显体现 Agent 能力，因为它不是单纯回答，而是在主动检查问题。

## 4. 结果展示方式

### 4.1 解读结果

使用信息卡，不用长篇聊天：

```text
安心无忧医疗险

适合人群：
适合作为社保补充，适合关注大额医疗费用报销的人群。

核心保障：
- 住院医疗费用
- 特殊门诊费用
- 门诊手术费用

需要注意：
- 既往症不赔
- 美容整形不赔
- 等待期 30 天

证据：
#45 核心责任
#46 等待期
#47 除外责任
```

### 4.2 对比结果

使用固定表格：

```text
安心无忧医疗险 vs 康宁保重疾险

| 维度 | 安心无忧医疗险 | 康宁保重疾险 |
| --- | --- | --- |
| 产品定位 | 医疗费用报销 | 重疾收入补偿 |
| 核心保障 | 住院、门诊、特殊门诊 | 重疾、中症、轻症 |
| 适合人群 | 需要医疗费用保障 | 需要大病收入补偿 |

Agent 建议：
两者不是替代关系，更适合组合配置。
```

每列都必须有对应产品自己的证据。证据不足时不要生成完整结论，而是提示：

> 康宁保重疾险的等待期证据不足，建议补充条款后再对比。

### 4.3 审计结果

使用风险报告：

```text
风险等级：中

发现 3 个注意点：

1. 既往症不赔
   证据：#47

2. 等待期 30 天
   证据：#46

3. 未发现保证续保条款
   状态：条款缺失

建议追问：
- 是否保证续保？
- 外购药是否有指定药房限制？
```

## 5. Agent 能力如何体现

不展示复杂技术名词，但展示“助理做了什么”。

结果区下方放一个轻量步骤条：

```text
已完成：
1. 识别产品
2. 查找条款
3. 整理证据
4. 检查引用
5. 生成结论
```

点击“查看依据”后展开：

```text
识别到产品：安心无忧医疗险
检索到条款：5 条
引用校验：通过
缺失信息：保证续保
```

这样既体现 Agent 的工作过程，又不会吓到普通用户。

同时增加一块轻量“检索过程”卡片：

```text
检索过程
检索问题：6
入模条款：5/7
覆盖：100%

等待期 -> #46
免责条款 -> #47
续保条件 -> #49
```

这块卡片是 RAG 的产品化表达：用户看到的不是技术 pipeline，而是“助理确实先查了证据再回答”。

## 6. 后端 Agent 设计

前端简单，后端要扎实。

参考 Claude Code Agent Internals，采用轻量状态机：

```text
User Task
  -> Create AgentRun
  -> Resolve Product
  -> Plan Task
  -> Retrieve Evidence
  -> Generate Draft
  -> Verify Citations
  -> Final Answer
```

### 6.1 工具对象

第一版只需要这些内部工具：

| 工具 | 作用 | 用户是否感知 |
| --- | --- | --- |
| `resolve_products` | 识别产品和别名 | 否 |
| `retrieve_clauses` | 检索条款 | 轻微感知为“查找条款” |
| `compare_products` | 生成对比结构 | 否 |
| `audit_product` | 检查风险和缺失项 | 感知为“风险检查” |
| `verify_citations` | 校验结论证据 | 感知为“引用已校验” |
| `build_report` | 生成结果报告 | 否 |

每个工具必须有：

- Zod input schema
- Zod output schema
- tool name
- latency
- status
- error message

### 6.2 可信度规则

这是第一版最重要的工程约束：

- 没有证据就说“条款未说明”。
- Compare 必须分别检索两个产品。
- Compare 的每个产品都必须有自己的引用。
- Audit 必须区分 `found`、`missing`、`risk`。
- 等待期、免责、续保这些高风险字段必须强制检查。
- citation verification 失败时最多重写一次。
- 重写后仍失败，就返回 warning，不强行给确定结论。

## 7. API 计划

保留当前 `/api/v2/search`，但新增面向产品任务的 API。

### 7.1 深入提问

`POST /api/agent/ask`

```json
{
  "productName": "安心无忧医疗险",
  "question": "既往症赔不赔？",
  "history": [
    { "question": "既往症赔不赔？", "answer": "既往症一般不予赔付。" }
  ]
}
```

返回：

```json
{
  "mode": "ask",
  "productName": "安心无忧医疗险",
  "question": "既往症赔不赔？",
  "shortAnswer": "string",
  "answer": "string",
  "keyPoints": [],
  "caveats": [],
  "followUps": [],
  "nextActions": [],
  "sources": [],
  "retrievalPlan": [],
  "retrievalStats": {},
  "agentSteps": []
}
```

### 7.2 解读产品

`POST /api/agent/explain`

```json
{
  "productName": "安心无忧医疗险"
}
```

返回：

```json
{
  "mode": "explain",
  "productName": "安心无忧医疗险",
  "summary": "string",
  "suitableFor": "string",
  "coverages": [],
  "exclusions": [],
  "warnings": [],
  "sources": [],
  "retrievalPlan": [],
  "retrievalStats": {},
  "agentSteps": []
}
```

### 7.3 对比产品

`POST /api/agent/compare`

```json
{
  "products": ["安心无忧医疗险", "康宁保重疾险"],
  "userProfile": "32岁，有社保，预算有限，更关心住院报销"
}
```

返回：

```json
{
  "mode": "compare",
  "products": [],
  "table": [],
  "summary": [],
  "recommendation": "string",
  "evidenceCoverage": {},
  "warnings": [],
  "nextActions": [],
  "sources": [],
  "retrievalPlan": [],
  "retrievalStats": {},
  "agentSteps": []
}
```

### 7.4 审计风险

`POST /api/agent/audit`

```json
{
  "productName": "安心无忧医疗险"
}
```

返回：

```json
{
  "mode": "audit",
  "productName": "安心无忧医疗险",
  "riskLevel": "medium",
  "findings": [],
  "missingItems": [],
  "suggestedQuestions": [],
  "sources": [],
  "retrievalPlan": [],
  "retrievalStats": {},
  "agentSteps": []
}
```

## 8. MVP 范围

### 必做

- 产品选择器
- 深入提问
- 解读产品
- 对比产品
- 审计风险
- 引用证据抽屉
- 检索过程卡片
- 简洁 Agent 步骤条
- Compare 多产品独立检索
- Audit 缺失项检测
- 引用校验

### 暂不做

- 复杂 Trace Lab
- 面向技术用户的 pipeline 参数配置
- 多轮自治 Agent
- 子 Agent
- 权限弹窗
- 复杂 Evaluation 面板
- 自定义大量对比维度
- 展示 token、模型、RRF、HyDE 等底层细节

这些能力可以保留在后台日志、README 或面试讲述里，不放进 non-tech 主界面。

## 9. 1-2 周交付计划

### 第 1-2 天：收敛产品体验

- 重构 `/chat` 首屏
- 四个任务卡：提问、解读、对比、审计
- 产品列表加载改成 skeleton
- 移除或隐藏复杂侧边栏入口

### 第 3-4 天：可信度修复

- Compare 改为多产品独立检索
- 修复引用 ID 不一致问题
- 增加 evidence coverage
- 证据不足时阻止生成强结论

### 第 5-6 天：Audit Agent

- 实现默认审计项：等待期、免责、续保、既往症、年龄限制
- 输出风险等级、风险点、缺失项、建议追问
- 增加引用校验

### 第 7-8 天：结果体验打磨

- 解读卡片
- 对比表格
- 风险报告
- 引用证据抽屉
- Agent 步骤条

### 第 9-10 天：演示和文档

- 准备四条演示路径
- README 补充启动和产品说明
- 截图和 demo script
- 健康检查和 Docker 验证

## 10. 演示路径

### 路径 1：深入提问

用户选择“安心无忧医疗险”，输入“既往症赔不赔？”，点击“提交问题”。

展示：

- 直接回答：既往症不予赔付
- 原文依据：#47 除外责任
- 检索过程：2 个检索问题，2/7 条款入模

讲述重点：

> 用户可以自由追问，但后端仍然先检索当前产品的相关条款，只把命中的证据交给模型。

### 路径 2：解读产品

用户选择“安心无忧医疗险”，点击“生成解读”。

展示：

- 核心保障
- 免责条款
- 等待期
- 引用证据

讲述重点：

> 这个 Agent 不只是生成摘要，而是先查条款，再用证据生成结构化解释。

### 路径 3：对比产品

用户选择“安心无忧医疗险”和“康宁保重疾险”，点击“开始对比”。

展示：

- 医疗险和重疾险不是替代关系
- 每个产品都有独立证据
- 生成组合建议

讲述重点：

> Compare 模式强制双产品独立检索，避免只凭一个产品的证据编造对比。

### 路径 4：审计风险

用户选择“安心无忧医疗险”，点击“检查风险”。

展示：

- 风险等级
- 既往症不赔
- 等待期
- 缺失续保信息
- 建议追问

讲述重点：

> Audit 模式体现 Agent 能力：它会主动检查风险和缺失信息，而不是被动回答问题。

## 11. 面试讲述主线

推荐这样讲：

1. 这个项目不是普通聊天机器人，而是保险条款分析 Agent。
2. 面向 non-tech 用户，所以前端只保留四个任务：提问、解读、对比、审计。
3. 后端使用 RAG 做证据治理，LLM 负责结构化总结和解释。
4. Compare 模式做多产品独立检索，避免伪对比。
5. Audit 模式检查风险、缺失项和引用一致性。
6. 大上下文时代，RAG 的价值不是“塞不下”，而是可追溯、可审计、可评估。
7. Agent 能力体现在任务规划、工具调用、证据校验和风险审计，但对用户保持简单。

## 12. 最终取舍

第一版不追求“看起来很技术”，而追求“普通人真的会用”。

最终判断标准：

- 用户是否 10 秒内知道怎么开始？
- 用户是否不用写 prompt 也能得到结果？
- 每个结论是否能看到原文依据？
- 对比是否真的有双方证据？
- 审计是否能指出风险和缺失？
- 面试时是否能讲清楚 Agent 的工程设计？

如果答案都是 yes，这个 MVP 就成立。
